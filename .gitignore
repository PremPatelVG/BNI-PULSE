node_modules/
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
*.err
.DS_Store
coverage/
dist/
firebase-service-account-base64.txt
.netlify/

# Generated credential lists - never commit these
member-pins*.csv
ambassador-pins*.csv

# SA login credential list - never commit
sa-logins*.csv

# Live DC logins used by the barter scripts - never commit (example file is safe)
scripts/.barter-test-accounts.json

# Generated chapter growth analysis - business data, keep local
# (regenerate: node scripts/analyze-netadd.js / node scripts/reverify-growth.js)
scripts/netadd-preview.csv
scripts/reverify-growth.csv

# Generated Word documents (regenerate: node docs/build_user_guides.js) + Word temp files
docs/*.docx
~$*.docx

# Pre-Core Commitment local dataset - prospect names + emails (PII), never commit
data/pre-core-commitments.*
# Derived Pre-Core net-add analysis - regional business data, keep local
data/precore-netadd-preview.csv
data/region-growth-preview.csv
data/induction-verification.csv

# Current members by chapter (derived from TLR) - regional business data, keep local
data/current-members-by-chapter.csv

# LOM match analysis - member names (PII), keep local
data/lom-under1yr-match.csv
data/lom-under1yr-refined.csv

# LOM cumulative match - member names (PII), keep local
data/lom-cumulative-under1yr.csv

# Miyagi reconcile plan - member names (PII), keep local
data/miyagi-reconcile-plan.csv

# Miyagi backup before reconcile - PII, keep local
data/miyagi-backup-*.json

# TLR PDF extraction - member names + scores (PII / business data), keep local
data/tlr/

# Python bytecode from the TLR extraction scripts
__pycache__/
*.pyc
