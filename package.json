{
  "name": "bni-pulse",
  "version": "1.0.0",
  "description": "BNI Pulse frontend and backend service",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/firebaseAdmin.js && node --check src/middleware/auth.js && node --check src/routes/auth.js && node --check src/routes/data.js && node --check src/services/firestore.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "firebase-admin": "^14.2.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pdfjs-dist": "^6.2.108"
  }
}
